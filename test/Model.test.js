const user = `/api/user`;

describe(`Test Case Sample`, () => {
  beforeEach(() => {
    jest.setTimeout(10000);
  });

//   test(`It should response the Successfully Created!!`, (done) => {
//     return request(app).post(`${user}/register`).send(obj)
//       .then((res) => {
//         const { body } = res
//         expect(res.statusCode).toBe(200)
//         expect(body.success).toBeTruthy()
//         done()
//       })
//   })
});
